# Project Guidelines & Rules

- **Git Push Policy**: Do NOT run `git push` automatically or push changes directly to remote repositories. Only apply changes locally and ask for/wait for user confirmation before pushing to Git remote branches.
